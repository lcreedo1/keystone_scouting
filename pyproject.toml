[build-system]
requires = ["setuptools>=61.0.0"]
build-backend = "setuptools.build_meta"

[project]
name = "keystone_scouting"
version = "0.1.0"
description = "Soccer analytics engineering data pipeline"
dependencies = [
    "pandas",
    "google-cloud-bigquery",
    "statsbombpy",
    "pyarrow"
]